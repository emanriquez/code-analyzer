click>=8.0.0
pyyaml>=6.0
gitpython>=3.1.0
requests>=2.28.0
openai>=1.0.0
google-generativeai>=0.3.0
# tomli is only needed for Python < 3.11 (tomllib is built-in in Python 3.11+)
# Install manually if needed: pip install 'tomli>=2.0.0; python_version < "3.11"'
tomli>=2.0.0
